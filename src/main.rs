fn main() {
    // println!() it is a macro that prints the string to the console
    //macro is used to output the string to the console
    //macros are similar to functions but they do not follow same rules as functions

    //rust compiler ignore spaces unlike python
    //the entire code can be written fnmain(){println!("Hello, world!");}

    
    println!("Hello, world!");
}
